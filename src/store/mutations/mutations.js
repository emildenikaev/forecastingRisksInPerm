export default {
  SET_GRAPH_TO_STATE: (state, graph) => {
    state.graph = graph;
  },
  SET_PREDICT_TO_STATE: (state, predict) => {
    state.predict = predict;
  },
  SET_INFO_TO_STATE: (state, info) => {
    state.info = info;
  },
  SET_ALLINFO_TO_STATE: (state, allinfo) => {
    state.allinfo = allinfo;
  },
  SET_COLORS_TO_STATE: (state, colors) => {
    state.colors = colors;
  },
};
